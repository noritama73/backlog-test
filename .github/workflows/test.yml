name: test

on:
  push:
    branches:
      - "master"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo github.event.pull_request 
        run: echo ${{ github.event.pull_request }}
